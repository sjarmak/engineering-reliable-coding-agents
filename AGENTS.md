# Agent instructions

## Failure-mode preventions

- Run the `no-ai-slop` skill on every public-facing documentation change before commit; skipping the pass leaves avoidable AI-writing patterns in release-facing prose.
